<nav class="navbar navbar-expand-lg btn btn-primary" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{path('app_home_page')}}">Accueil</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav me-auto">
            <li class="nav-item">
                <a class="nav-link active" href="{{path('app_login')}}">Se connecter
                    <span class="visually-hidden">(current)</span>
                </a>
            </li> 
            <li class="nav-item">
                <a class="nav-link active" href="{{path('app_logout')}}"> Déconnexion
                    <span class="visually-hidden">(current)</span>
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="{{path('app_register')}}">S'inscrire
                    <span class="visually-hidden">(current)</span>
                </a>
            </li>
            {# <li class="nav-item">
                <a class="nav-link active" href="{{path('app_page_user')}}"> Users
                    <span class="visually-hidden">(current)</span>
                </a>
            </li> #}

            {# {% if is_granted("ROLE_ADMIN") %}
            <li class="nav-item">
                <a class="nav-link active" href="{{path('app_dashboard_admin')}}"> Tableaux de bord Admin
                    <span class="visually-hidden">(current)</span>
                </a>
            </li>
            {% endif %} #}

            <li class="nav-item dropdown">
                <a class="nav-link active dropdown-toggle" data-bs-toggle="dropdown" href="{{path('app_allCategories')}}" role="button" aria-haspopup="true" aria-expanded="false">Catégories</a>
                <div class="dropdown-menu">
                    <a class="dropdown-item" href="{{path('app_allCategories')}}">Toutes</a>
                    <a class="dropdown-item" href="{{path('app_category_new')}}">New Cat</a>
                    {# <a class="dropdown-item" href="{{path('app_update_category')}}">Modifier cat</a> #}
                    <a class="dropdown-item" href="#">Vetements</a>
                    <a class="dropdown-item" href="#">Snakers</a>
                    <a class="dropdown-item" href="#"></a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="#">Pulls</a>
                </div>
            </li>
            
        </ul>
        <form class="d-flex">
            <input class="form-control me-sm-2" type="search" placeholder="Search">
            <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
        </form>
        </div>
    </div>
</nav>