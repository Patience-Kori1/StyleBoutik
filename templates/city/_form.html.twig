{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-outline-primary mt-4 mb-2">{{ button_label|default('Enregistrer') }}</button>
    <button class="btn btn-outline-primary mt-4 mb-2">{{ button_label|default('Retour à la liste') }}</button>
{{ form_end(form) }}
{# <div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h2 class="h4 mb-0">Créer une nouvelle ville</h2>
                </div>

                        {{ form_start(form) }}
                            <label for="">Nom de la ville</label>
                            {{ form_widget(cities.name, {attr:{'class': 'form form-control'}}) }}
                            <label for="">Frai de livraison </label>
                            {{ form_widget(cities.shippingCost, {attr:{'class': 'form form-control'}}) }}
                            <button class="btn btn-outline-primary">{{ button_label|default('Sauvegarder') }}</button>
                        {{ form_end(form) }}

            </div>
        </div>
    </div>
</div> #}